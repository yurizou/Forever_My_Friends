<div class="header bg-secondary text-white">
    <div class="row justify-content-center pt-5">
      <h1 class="text-center">Forever My Friends<br><span class="mt-3">〜〜大切な人との素敵な思い出を刻もう〜〜</span></h1>
    </div>
</div>
<style>

</style>
<div class="container mb-5">
  <% if current_user %>
    <div class="row">
      <div class="mt-5 col-1 offset-3">通知</div>
      <div class="mt-5 col-5">
        <% if @birthday_notices.any? %>
          <p>誕生日の通知</p>
          <ul>
            <% @birthday_notices.each do |notice| %>
              <li>今日は<%= notice.friend.nickname %>さんの誕生日です！</li>
            <% end %>
          </ul>
        <% end %>
        
        <% if @meet_friend_notices.any? %>
          <p>予定日の通知</p>
          <ul>
            <% @meet_friend_notices.each do |notice| %>
              <li>今日は<%= notice.friend.nickname %>さんと会う日です！</li>
            <% end %>
          </ul>
        <% end %>
        
        <% if @neglect_friend_notices.any? %>
          <p>疎遠の通知</p>
          <ul>
            <% @neglect_friend_notices.each do |notice| %>
              <li><%= notice.friend.nickname %>さんと最近連絡を取っていませんか？</li>
            <% end %>
          </ul>
        <% end %>
      </div>
    </div>
  <% end %>

  <div class="row my-3">
    <div class="col-10 mb-5 offset-2">
      <table class="table">
      <tbody>
        <tr>
          <th>日付</th>
          <th>管理人からお知らせ</th>
          <th>詳細</th>
        </tr>
         <% @topics.each do |topic| %>
        <tr>
           <th><%= topic.created_at.strftime("%Y/%m/%d") %></th>
           <th><%= topic.title %></th>
           <th><%= topic.article %></th>
        </tr>
        <% end %>
      </tbody>
    </table>
    </div>
  </div>
</div>
